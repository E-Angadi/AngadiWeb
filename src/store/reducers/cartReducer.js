const initstate = {
  items: [],
  mrpTotal: 0,
  discount: 0,
  total: 0,
};

const cartReducer = (state = initstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cartReducer;
